spring:
  cloud:
    nacos:
      discovery:
        ip: 192.168.8.5
        server-addr: ${nft.turbo.nacos.server.url}
        networkInterface: 192.168.8.5
      config:
        server-addr: ${nft.turbo.nacos.server.url}
        file-extension: properties
        name: ${spring.application.name}
    inetutils:
      preferredNetworks:
        - 192.168.8.5